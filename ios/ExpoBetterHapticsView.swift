import ExpoModulesCore

// This class is a placeholder since we don't need a view for haptics functionality
class ExpoBetterHapticsView: ExpoView {
    required init(appContext: AppContext? = nil) {
        super.init(appContext: appContext)
    }
}
